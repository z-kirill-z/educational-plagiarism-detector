![Tests and Lint](https://github.com/z-kirill-z/educational-plagiarism-detector/actions/workflows/tests.yml/badge.svg)

# Educational Plagiarism Detector

🎓 **Educational Plagiarism Detector** — учебный проект для обнаружения текстового сходства (потенциального плагиата) в студенческих работах с использованием классических алгоритмов обработки текста.

Проект ориентирован на **образовательные цели** и демонстрирует:
- базовую обработку текстовых данных,
- сравнение документов несколькими алгоритмами,
- написание unit-тестов,
- автоматическую проверку качества кода через CI.

---

## 🚀 Основные возможности

- 📂 Загрузка и обработка текстов из файлов:
  - `.txt`
  - `.pdf`
- 🧹 Предварительная обработка текста:
  - приведение к нижнему регистру,
  - удаление пунктуации,
  - токенизация по пробелам,
  - удаление стоп-слов
- 🔍 Сравнение документов несколькими методами:
  - **Cosine similarity (TF-IDF)**
  - **Longest Common Subsequence (LCS)**
  - **N-gram matching**
- 🕒 Сохранение результатов анализа в JSON с временной меткой
- 🤖 Автоматическая проверка проекта через GitHub Actions:
  - `flake8`
  - `pytest`

---

## 🧠 Используемые методы

### 1. Cosine Similarity + TF-IDF
Оценивает степень сходства текстов на основе частот слов с использованием векторного представления TF-IDF и косинусной меры.

### 2. Longest Common Subsequence (LCS)
Определяет длину наибольшей общей подпоследовательности слов между двумя текстами.

### 3. N-gram Matching
Сравнивает тексты по пересечению n-грамм (по умолчанию используется `n = 3`).

---

## 🧹 Предварительная обработка текста

Предобработка реализована **без использования NLTK** и включает:

- приведение текста к нижнему регистру;
- удаление пунктуации с помощью регулярных выражений;
- разбиение текста на токены через `str.split`;
- удаление базового набора английских стоп-слов.

Это сделано для:
- простоты установки проекта,
- отсутствия внешних NLP-ресурсов,
- стабильной работы в CI.

---

## 📁 Структура проекта

```text
.
├── src/                    # Основной код проекта
│   ├── loader.py            # Загрузка .txt и .pdf файлов
│   ├── preprocessing.py    # Очистка и нормализация текста
│   ├── similarity.py       # Алгоритмы сравнения текстов
│
├── tests/                  # Unit-тесты
│   ├── test_loader.py
│   ├── test_preprocessing.py
│   └── test_similarity.py
│
├── uploads/                # Загружаемые студенческие работы
├── results/                # Результаты анализа (JSON)
├── .github/workflows/      # CI/CD (flake8 + pytest)
├── requirements.txt
├── README.md
└── .gitignore
```

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/educational-plagiarism-detector.git
cd educational-plagiarism-detector
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск тестов
```bash
pytest
```

### 4. Проверка стиля кода
```bash
flake8 .
```

---

## 🧪 Тестирование

Проект покрыт unit-тестами, которые проверяют:
- корректную загрузку файлов;
- работу предобработки текста;
- правильность расчёта метрик сходства.

Тесты автоматически запускаются в **GitHub Actions** при каждом коммите в репозиторий.

---

## 📌 Ограничения проекта

- Проект не является промышленной системой анти-плагиата.
- Используются упрощённые алгоритмы без глубокого семантического анализа.
- Проект предназначен для учебных и демонстрационных целей.

---

## 📜 Лицензия

Проект распространяется в учебных целях.
Может свободно использоваться и модифицироваться для обучения.
